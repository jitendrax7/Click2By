import React from 'react'

// Search functionality is integrated into the Navbar component.
// This component is kept as a no-op to avoid import errors from any
// pages that still reference it.
const SearchBar = () => null

export default SearchBar
